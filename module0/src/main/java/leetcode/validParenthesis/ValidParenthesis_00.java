package leetcode.validParenthesis;

public class ValidParenthesis_00 {
    //(()()(q()p))
    //if elem in [, (, {,q  stack.add(el)
    //elem - closing - stack.pop => closing_bracket; + element == pair
//        pair (), {}, [], qp
//    ) stack == ( =>res is ()

    //arithmetic expression calculator
    // expression: -1*2+1*(4-10)*9/100; method recursive spusk
    // обратаня польская запись
}
